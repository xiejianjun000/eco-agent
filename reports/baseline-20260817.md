# ECO AGENT 基线体检报告（2026-08-17）

> 执行：DSH 会话 · 依据 `docs/724-verification-plan.md` L2 层 · 全程离线零 API 成本
> 结论速览：**核心代码健康（单测 100% 绿、审计链 456 条完整），工具链 4 个 bug 已当场修复；知识层规范欠账 127 项，后台循环（进化/记忆/心跳）本机从未真实产出——这是下一阶段长稳验证的重点。**

## 1. 结果汇总

| 体检项 | 结果 | 说明 |
|--------|------|------|
| 单元测试（tests/ 全量） | ✅ 100% 通过（2 跳过） | 修复 1 个真 bug 后全绿 |
| lint 项目健康 | ⚠️ 218→127 | 工具自身 3 个 bug 已修；余 127 项为知识层真实欠账 |
| quality_audit 14 维 | ⚠️ 11/14 满分 | D1 缺文件、D3 frontmatter 13%、D8 评分口径有误 |
| verify_ops 运维体检 | ⚠️ 部分 | SM3 审计链 456 条✅；进化报告缺失、记忆树 0 节点、技能库 0 |
| evals 45 题回归 | 未跑 | 需真实 LLM 调用（消耗 DeepSeek 配额），待批准 |
| API / Web UI 冒烟 | 未跑 | 需启动 `eco server`，待批准 |

## 2. 本轮已修复（4 个 bug）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `server/api/chat.py:391` | LLM 降级模式返回 `ttft_ms=None` → pydantic 校验 500 | `or 0` 兜底，单测 `test_chat_without_llm_degrades` 转绿 |
| 2 | `_scripts/lint.py` | Windows 命令 `cd /d` 在 mac/linux 报错污染输出 | 改跨平台 `git -C` subprocess |
| 3 | `_scripts/lint.py` | 大文件检查误扫 node_modules/.libs/字体资产/审计数据 | 加 SKIP_DIRS + 资产白名单 |
| 4 | `_scripts/lint.py` | 原文指针/frontmatter 检查套用到工程文档 | 限定知识层文件（memory-tree/skills/ecoskills/kb） |

## 3. 真实欠账（内容维护，非代码缺陷）

- **知识层规范**：74 个知识文件缺 `## 原文指针` 段、55 个缺 frontmatter（含 eco-codex 法典 kb 5 编、gongwen-draft 引用文件）。
  ⚠️ 法典 kb 条文文件是否需要"原文指针"属规范政策问题（条文本身即原文），待定后由 statute_updater 统一生成。
- **`开发实施方案.md` 缺失**：lint 必备清单和 quality_audit D1 都点名它，需补文件或调整清单。
- **quality_audit 自身口径待修**：D3 误扫 `.libs/` 依赖许可证；D8 在"未扫到链接"时误判 FAIL（实际 0 断链）。

## 4. 关键发现：后台循环从未真实产出

`verify_ops` + `~/.eco` 证据显示：运行时在本机跑过（decisions.jsonl 75KB、SM3 链 456 条），但
**进化报告缺失、记忆树 0 节点、技能库 0 个**——L3 心跳/L4 进化/记忆系统在真实环境从未产出过可见结果。
这正是 `benchmarks/longrun_pulse_evolve.py` 要观测的"README 声称 vs 实测"差距，列入 L3 周检最高优先级。

## 5. 待批准项（消耗配额/起服务）

1. `ECO_EVAL=1 evals.runner --baseline ...` 跑 45 题基线（首次建立回归红线）
2. 启动 `eco server` → API 端到端 + playwright Web UI 巡检
3. longrun --smoke 压缩时序自检（120s）

## 6. 下一步（按 724 方案落地三步）

- [ ] 补 `_scripts/smoke_tools.py`（每工具 1 条真用例）
- [ ] 补 `_scripts/patrol.py`（L0+L1 一键巡检 + 飞书告警）
- [ ] 补/删 `开发实施方案.md`、定 kb 文件 frontmatter 政策、修 quality_audit 口径
- [ ] 服务器部署 systemd + crontab + 告警群
