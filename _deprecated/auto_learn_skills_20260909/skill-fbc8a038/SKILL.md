---
name: skill-fbc8a038
description: Use when 退出后，我不会重新启动你, especially when using `shell_run`, `shell_run`, `shell_run`.
---

# skill-fbc8a038

Use when 退出后，我不会重新启动你, especially when using `shell_run`, `shell_run`, `shell_run`.

## Workflow / 工作流

1. shell_run
2. shell_run
3. shell_run
4. shell_run
5. shell_run
6. shell_run

**Tools**: `shell_run`, `shell_run`, `shell_run`, `shell_run`, `shell_run`, `shell_run`

## Inputs and outputs / 输入与输出

- 输出示例：✅ **不用重启也能收口——本轮把 `mcp_count=0` 的代码级根因挖到了：它是个“假读数”（reload 从不清理旧注册，重连成功也计 0）；配置层已实测健康（17/17 解析成功、8004

## Boundaries / 边界与排除

- 只在本技能声明的范围内工作；超出范围时明确说明并停止。
- 不修改与任务无关的文件；改动任何文件前先读取目标内容。
- 涉及持久化产物时，写出目录/格式遵循用户或项目的既有约定。

## Example / 示例

> 请用 "skill-fbc8a038" 技能处理：<输入样例>

按"工作流"逐步执行，并在过程中报告关键中间结果与最终产出。
