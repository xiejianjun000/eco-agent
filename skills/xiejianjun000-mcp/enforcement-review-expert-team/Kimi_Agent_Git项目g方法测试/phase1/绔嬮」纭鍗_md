# 立项确认单 · 平台管理「自助添加平台」（前端扩展）

> v2.0（Kimi 合规版）｜ 2026-08-07
> 状态：待主理人确认（流程唯一交互点）

---

## 一、这次纠偏了什么（v1.0 → v2.0）

| v1.0（已废弃）的问题 | v2.0（Kimi 合规）的修正 |
|---|---|
| 状态模型自创「未接通」，抹掉 Kimi 的「待人工首次登录」 | **原样沿用 Kimi 四态**：AI代管中 / 待人工首次登录 / 接入配置中 / 异常 |
| 另起 5 步向导，架空 Kimi 三步引导弹窗 | 在 Kimi 三步引导**前置**插入"粘贴地址→匹配"一步，落回原三步 |
| 丢概览条 / 巡检报告区，没落进三栏布局 | 严格沿用 platforms.md 页面骨架（概览条 + 2列卡 + 巡检报告区） |
| 抽象成"白名单首批6个"，弱化真实数据 | 保留 Kimi **真实 6 平台**名称与状态数据 |
| ARCHITECTURE 提"新建 FastAPI+PostgreSQL 后端" | **后端 = Hermes agent（AI 基座）**，不新建服务/数据库/反向代理 |
| 满是代理/iframe/AES-256/MFA 黑话 | 遵守 Kimi 非技术化铁律，黑话仅内部、不外露 |

---

## 二、我们要建的是什么

在 Kimi 既有「平台管理」模块里，增加**自助添加平台**能力：

> 执法人员粘贴一个已备案环保执法平台地址 → 系统认出它 → 生成 EcoAegis 风格登录界面 → 验证码推到这个界面 → 登录一次后进入「AI代管中」，之后 AI 代管日常操作与巡检。

**前端为主**（React18+Vite5+TS strict+纯CSS，沿用三栏布局、design.md Token、src/data）；**后端 AI 基座 = Hermes agent**（白名单匹配 / 登录壳字段生成 / 验证码抓取识别 / 会话保活）。

---

## 三、关键决策（4 项，沿用并收敛）

| 决策 | 内容 |
|---|---|
| D1 界面形态 | 自建 EcoAegis 风格登录壳（前端渲染，非原站样式、非 iframe） |
| D2 接入机制 | 已备案白名单 + 预置字段模板（每平台一套，非任意网址自适应） |
| D3 凭据安全 | 本机加密存储，不出本机 |
| D4 验证码 | AI（Hermes 基座）识别 + 复杂验证码人工兜底 |

---

## 四、平台四态（Kimi 原样，三文档统一）

| 状态 | 颜色 | 含义 |
|---|---|---|
| `AI代管中` | 橄榄绿 `#7C8B5F` | 已接通、心跳正常、AI 日常代管（稳态） |
| `待人工首次登录` | 琥珀 `#C9A24B` | 模板已匹配、等待人工登录一次 |
| `接入配置中` | 灰蓝 `#6E8299` | 模板匹配 / 配置进行中 |
| `异常` | 赤陶红 `#B0574A` | 登录失败 / 会话过期 / 探活异常 |

（无"未接通"态；"已接通"在概览条/操作区作分类用语，其持久态即 `AI代管中`。）

---

## 五、MVP 范围（前端闭环）

- **F1** 自助添加入口（粘贴地址 + 白名单匹配）
- **F2** EcoAegis 风格登录壳渲染（前端按模板生成）
- **F3** 验证码抓取与 AI 识别兜底（Hermes 基座）
- **F4** 接通状态管理 + 凭据本机加密存储

**P1（后续）**：代管范围开关、暂停/恢复、失败诊断、机构级恢复、巡检联动、MFA 人工受控首登。

**硬边界**：仅白名单内"账密 + 图形验证码"型平台；UKEY/证书/人脸/扫码类 MFA 不自动接入。

---

## 六、交付物（本轮重写）

- `phase1/PRD.md` v2.0
- `phase1/ARCHITECTURE.md` v2.0
- `phase1/UIUX.md` v2.0

---

## 七、需主理人确认

1. 范围、四态、硬边界认不认？
2. 后端收敛为 Hermes agent（不新建服务栈）认不认？
3. MVP（F1–F4）砍项或调整？

确认后自动推进：**Spec 生成 → 设计细化 → 并行开发 + 自检 → 测试交付**。
