# 入库质量清零 + 两个决策点同步完成（2026-07-17）

## 背景
用户确认执行建议：同步完成"决策1(批量补 frontmatter)"与"决策2(tags 双链转纯文本)"，并修 lint 旧法误报、清 A 类悬空双链，最终让入库质量达标（lint 归零）。

## 执行过程（按纪律：先诊断 → 脚本化 → 人工核验 → git 提交）

### 1. 关键认知纠正
- 原文 60 篇"缺 frontmatter"实为误判：旧库文档都有 `---` 块，只是用 Obsidian 格式（title/date/url/source/tags），未注入新库 schema。
- 101 悬空双链中，仅在 frontmatter `tags:` 下的 `[[标签]]` 可机械转纯文本（YAML 内 `#` 是注释符，故转纯文本列表项，非 `#标签`）；正文内悬空需单判。

### 2. 两个决策点（normalize_schema.py 同脚本一次跑完）
- 决策1：补 57 篇 schema 缺失字段 + 20 篇缺 title（用文件名补），共 77 篇；保留原 title/date/url/source/tags，正文零改动。
- 决策2：18 篇 frontmatter 下 `[[标签]]` → 纯文本。

### 3. A 类悬空双链（fix_dangling.py 定点修 7 篇，不碰合法链接）
- 跨库链接 `生态环境知识库/.../README` → 去括号改纯文本
- 真实页修路径（`湖南省省级环保督察体系总览`→`20_...`、`评查细则全文2024`→`评查细则全文2024指针` 等）
- 其余标签（`实战全流程`/`相关子文档` 等）→ `#标签`

### 4. 旧法引用人工核验（11 处 lint 初报）
- 逐条打开上下文判定：全部合法（讲废止/衔接/案例原文/反例教学/法典化后见法典），**0 真红线**。
- lint 加固：旧法豁免白名单（讲废止/替代/衔接/法典化后/案例原文/反例教学语境放行）；标题中英文 key 都认；忽略反引号代码段内双链举例——根治误报。

### 5. SCHEMA 固化
- SCHEMA.md 增补"六、入库标准动作"：reindex / normalize_schema / fix_dangling / lint 四脚本必跑，目标 lint 归零。

## 结果
- **lint 终检：frontmatter 0 / 悬空 0 / 孤页 0 / 旧法红线 0（总计 0）**
- 全库 153 篇，git 提交 12 次，log 8 条。
- 入库质量从"161 项待清（含误报）"收敛至真正 0 项可复现、可体检、可提交。

## 结论
入仓（旧库 112 篇原始素材整批搬入）→ 入库（按 LLM Wiki 结构组织 + schema 统一 + 双链收敛 + 红线体检）全链路现已端到端跑通且质量达标。今后每次 Ingest/维护后按 SCHEMA"六"跑四脚本即可维持 lint 归零。
