---
name: skill-9a3ff7d4
description: Use when 到本地电脑去找, especially when using `shell_run`, `shell_run`, `shell_run`.
---

# skill-9a3ff7d4

Use when 到本地电脑去找, especially when using `shell_run`, `shell_run`, `shell_run`.

## Workflow / 工作流

1. shell_run
2. shell_run
3. shell_run
4. shell_run
5. shell_run
6. shell_run

**Tools**: `shell_run`, `shell_run`, `shell_run`, `shell_run`, `shell_run`, `shell_run`

## Inputs and outputs / 输入与输出

- 输出示例：✅ **全盘（桌面/下载/文档）已检索完毕，没有名为《MCP使用手册.md》的文件**——含"MCP"的文件有上百个，但没有一个叫"MCP使用手册"。

## 一、检索范围与结果

| 检索式 | 范

## Boundaries / 边界与排除

- 只在本技能声明的范围内工作；超出范围时明确说明并停止。
- 不修改与任务无关的文件；改动任何文件前先读取目标内容。
- 涉及持久化产物时，写出目录/格式遵循用户或项目的既有约定。

## Example / 示例

> 请用 "skill-9a3ff7d4" 技能处理：<输入样例>

按"工作流"逐步执行，并在过程中报告关键中间结果与最终产出。
