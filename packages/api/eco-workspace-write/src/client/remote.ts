/**
 * This package's generated Remote contribution, re-exported as a local module.
 *
 * The published subpath is `@deepseek-ai/dsh-api-eco-workspace-write/remote`;
 * importing it through this file keeps the client bundle's dependency on the
 * artifact explicit at this package's own boundary, and gives the browser build
 * one place to resolve it from.
 */
import ecoWorkspaceWriteRemote from '@deepseek-ai/dsh-api-eco-workspace-write/remote'

export { ecoWorkspaceWriteRemote }
export default ecoWorkspaceWriteRemote
