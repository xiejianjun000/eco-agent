/** Locale bundles for the eco quote-into-composer control. */

/** Locale keys this control shows. */
export type QuoteKey = 'action.label'

/** Namespace the dictionaries register under. */
export const NS = 'ecoQuote'

/** English copy. */
export const en: Record<QuoteKey, string> = {
  'action.label': 'Quote into the composer',
}

/** Simplified Chinese copy. */
export const zh: Record<QuoteKey, string> = {
  'action.label': '引用到输入框',
}
